import page from '../pages/page'
import hero, {HeroSpeakers, HeroStatistic} from './hero'
import {link} from './link'

export const schemaTypes = [hero, HeroStatistic, HeroSpeakers, link, page]

export const contentBlocks = [hero, link]
